import { damageSystem } from "./damageSystem.js";

export function collisionSystem(ctx) {
    //super basic collision
    //check if player is colliding with monsters
    // if it is, then take damage?
    // ceck if bullets collide with monsters perhaps, and if it does, take damage
    for( let enemy of ctx.enemies) {
        //is the distance to ctx.player.movement.position less than 1 perhaps?
    }
}